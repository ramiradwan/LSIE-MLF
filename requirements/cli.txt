# LSIE-MLF Operator CLI + Desktop GUI Requirements
#
# These are operator-host tools, NOT container dependencies. The API
# Server and ML Worker images do not need these packages. Install
# locally with:
#
#   pip install -r requirements/cli.txt
#
# §4.E.1 — Operator tooling: CLI, PySide6 Debug Studio (developer-facing),
# and the PySide6 Operator Console (production-grade, SPEC-AMEND-008).
#
# Per SPEC-AMEND-008, the Operator Console talks to the API Server only —
# it does NOT open direct Postgres or Redis connections, so those drivers
# stay out of this file. Pydantic is pinned here explicitly: the console
# validates every /api/v1/operator/* response into a shared DTO before
# it crosses the transport boundary (see services/operator_console/api_client.py)
# and pip would not otherwise pull pydantic onto an operator host that has
# no API-server install next to it.

typer>=0.12,<1.0
rich>=13,<15
PySide6>=6.11,<7.0
pydantic>=2,<3
opencv-python>=4.10,<5.0
mediapipe==0.10.9
numpy==1.26.*
psutil>=5.9
