# LSIE-MLF Operator CLI + Desktop GUI Requirements
#
# These are operator-host tools, NOT container dependencies. The API
# Server and ML Worker images do not need these packages. Install
# locally with:
#
#   pip install -r requirements/cli.txt
#
# §4.E.1 — Operator tooling (CLI + PySide6 Debug Studio).

typer>=0.12,<1.0
rich>=13,<15
PySide6>=6.7,<7.0
opencv-python>=4.10,<5.0
mediapipe==0.10.9
numpy==1.26.*
psutil>=5.9
